spring.application.name=sim-api

spring.datasource.url=jdbc:h2:file:./simulacao
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=admin
spring.datasource.password=Password
spring.jpa.hibernate.ddl-auto=update

# Configurações padrão do JPA (Adicione estas linhas)
spring.jpa.properties.jakarta.persistence.jdbc.url=jdbc:h2:file:./simulacao
spring.jpa.properties.jakarta.persistence.jdbc.driver=org.h2.Driver
spring.jpa.properties.jakarta.persistence.jdbc.user=admin
spring.jpa.properties.jakarta.persistence.jdbc.password=Password

#h2 console para testes
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# colocar juros com 6 casas decimais
spring.jackson.serialization.write-big-decimal-as-plain=true


# Credenciais do MS EventHub (lidas de variáveis de ambiente)
# Usa a variável de ambiente se existir, senão, usa o valor padrão
azure.eventhub.connection-string=${AZURE_EVENTHUB_CONNECTION_STRING:Endpoint=sb://eventhack.servicebus.windows.net/;SharedAccessKeyName=hack;SharedAccessKey=HeHeVaVqyVkntO2FnjQcs2Ilh/4MUDo4y+AEhKp8z+g=;EntityPath=simulacoes}
azure.eventhub.event-hub-name=simulacoes

# Expõe todos os endpoints do Actuator, incluindo o de métricas
management.endpoints.web.exposure.include=*

# SQL Server DataSource para migração de dados (lidas de variáveis de ambiente)
# Usa a variável de ambiente se existir, senão, usa o valor padrão
sqlserver.datasource.url=${SQLSERVER_DATASOURCE_URL:jdbc:sqlserver://dbhackathon.database.windows.net:1433;databaseName=hack}
sqlserver.datasource.username=${SQLSERVER_DATASOURCE_USERNAME:hack}
sqlserver.datasource.password=${SQLSERVER_DATASOURCE_PASSWORD:Password23}
sqlserver.datasource.driver-class-name=${SQLSERVER_DATASOURCE_DRIVER_CLASS_NAME:com.microsoft.sqlserver.jdbc.SQLServerDriver}
